<!doctype html>
<html class="no-js">
  <head>
    <meta charset="utf-8">
    <meta name="description" content="">
    <meta name="viewport" content="width=device-width, initial-scale=1, minimal-ui">
    <title>Iron Yard Sugar</title>
    <!-- Place favicon.ico and apple-touch-icon.png in the root directory -->
      <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>

  </head>
  <body>

    <header>
      <div class="header-container">
        <div class="header-text-overlay">
        <div class="header-text">
          <h1>Durham, NC</h1>
          <h2>Summer 2014 Cohort</h2>
        </div>
      </div>

    </header>
    <nav>
      <ul>
        <li>Filter</li>
        <li>Edit My Profile</li>
      </ul>
    </nav>
    <div class='container'>
    </div>

    <!-- Divs to store templates -->
    <div id = "list"></div>
    <div id = "homepage"></div>
    <div id = "single"></div>

<script type="text/template" class="user-template">
Hello
           <%% userData[idx].student.name %>
 </script>

<script type="text/javascript">
$.getJSON('/students.json').done(function(userData){
    console.log(userData);
    var userView = $('.user-template').html();
      _.each(userData, function(student) {
          $('.container').append(_.template(userView,userData));
        })
});

</script>
</body>
</html>
